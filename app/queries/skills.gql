query skills {
    Skills(limit: 100, where: { showcase: { equals: true } }) {
        docs {
            id
            name
            experience
            logo {
                url
                alt
            }
        }
    }
}
